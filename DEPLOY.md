# دیپلوی رایگان و always-on آنستریم

> **Railway** (بدون کارت، ۵ دلار اعتبار Trial، تک‌کانتینر + Volume):
> راهنمای قدم‌به‌قدم در `RAILWAY.md` — بیلد با `Dockerfile.railway`.

هدف: سایت **همیشه در دسترس** باشد، **بدون هزینه**، و **وابسته به روشن ماندن لپ‌تاپ نباشد** — و کتابخانه/آمار/دانلودها **بعد از ری‌استارت بمانند** (دیسک ماندگار).

## چرا نه Vercel؟

Vercel فقط **فرانت‌اند استاتیک** را نگه می‌دارد. بک‌اندِ آنستریم چیزهایی می‌خواهد که Vercel serverless ندارد:

- **دیسک ماندگار** — SQLite، کاورها، فایل‌های دانلودشده. روی Vercel با هر cold start پاک می‌شوند.
- **پروسه‌ی دائمی** — uvicorn، سرور PO Token، بات تلگرام. serverless تابع است، نه سرویس.
- **دانلود طولانی + SSE** — دانلود آلبوم از تایم‌اوت تابع بیشتر است و نوار پیشرفت (SSE) روی بافر Vercel می‌شکند.

و خودِ اپ طوری ساخته شده که فرانت و بک‌اند **یک مبدأ** باشند (`nginx` هر دو را سرو می‌کند). جداکردنشان روی دو دامنه، دوباره CORS و محدودیت‌های cross-origin را برمی‌گرداند. پس **کل پشته یک‌جا** می‌رود روی یک VM؛ Vercel حذف می‌شود.

## گزینه‌های VM رایگانِ always-on (با دیسک ماندگار)

| سرویس | رایگان | always-on | منابع | احراز هویت |
|---|---|---|---|---|
| **Oracle Cloud Always Free** ⭐ | $0 واقعی، همیشه | بله | ۴ هسته ARM + ۲۴GB رم + ۲۰۰GB دیسک | کارت (hold موقت ~۱$ برگشت‌پذیر) |
| **Google Cloud e2-micro** | always-free | بله | ۱GB رم (کم) | کارت (hold موقت برگشت‌پذیر) |

> **درباره‌ی کارت:** هر دو سرویس برای احراز هویت یک **authorization hold موقت (~۱ دلار)** روی کارت می‌گذارند که **بعد برمی‌گردد** — شارژ واقعی نیست. ولی کارتی که **صفرِ صفر موجودی** دارد معمولاً این hold را **رد می‌کند** و احراز هویت شکست می‌خورد. Oracle کارت‌های prepaid/مجازی را هم کلاً رد می‌کند. **واقع‌بینانه:** برای پاس شدن احراز هویت یا باید چند دلار موجودی در کارت باشد (همان hold برگشت‌پذیر است)، یا سراغ GCP بروی که احراز متفاوتی دارد. اگر هیچ‌کدام نشد، تنها راهِ بدون کارت **Hugging Face Space** است که دیسکش ephemeral است (تاریخچه پاک می‌شود) — با خواسته‌ی «ماندگاری» تضاد دارد.

پیشنهاد: **اول Oracle Always Free را امتحان کن** (بیشترین منابع رایگان). اگر کارت رد شد، GCP.

---

## مسیر A — سریع و موقت (برای اولین تست)

بدون دامنه، بدون تنظیمات. یک تونل **موقتی** trycloudflare آدرس HTTPS می‌دهد. روی VM (Ubuntu 22.04/24.04):

```bash
curl -fsSL https://raw.githubusercontent.com/mahan-mgn/Unstream/main/deploy/bootstrap.sh | bash
```

اسکریپت: داکر نصب می‌کند → مخزن کلون می‌شود → ایمیج‌ها بیلد می‌شوند → پشته با تونل بالا می‌آید → آدرس `https://…trycloudflare.com` را چاپ می‌کند. همان آدرس را در مرورگر باز کن.

**سقف مسیر A:** آدرس trycloudflare با هر ری‌استارت تونل **عوض می‌شود** و برای PWA/سرویس‌ورکر پایدار نیست. فقط برای «یک بار بالا بیاورم و ببینم کار می‌کند» خوب است. برای آدرس همیشگی → مسیر B.

---

## مسیر B — پایدار و رایگان (آدرس ثابت با HTTPS)

یک **Named Tunnel** در Cloudflare، که آدرس را به یک ساب‌دامین ثابت وصل می‌کند. دو زیر-مسیر:

### B1 — اگر دامنه داری (یا یک دامنه‌ی free می‌گیری)

1. دامنه‌ی free بدون کارت: **us.kg** (Nic US، نیاز به GitHub) یا **eu.org**. ثبت کن، چند روز تا تأیید طول می‌کشد.
2. دامنه را به **Cloudflare** اضافه کن (پلن Free کافی است). nameserverها را به Cloudflare تغییر بده.
3. در VM، یک Named Tunnel بساز:
   ```bash
   docker run --rm -v "$HOME/.cloudflared:/root/.cloudflared" cloudflare/cloudflared:latest tunnel login
   # یک برگه‌ی مرورگر باز می‌کند؛ دامنه‌ات را انتخاب کن تا cert.pem بیفتد
   docker run --rm -v "$HOME/.cloudflared:/root/.cloudflared" cloudflare/cloudflared:latest tunnel create unstream
   # credentials و config می‌سازد
   ```
4. تونل را به پورت ۸۰۸۰ کانتینر `web` وصل کن (یک `route dns` + اجرای دائمی با `restart: unless-stopped`).
5. ساب‌دامین نهایی، مثلاً `music.your-domain.us.kg`، همیشه همان است.

### B2 — بدون دامنه (IP خالی + Let's Encrypt)

اگر نمی‌خواهی دامنه بگیری: VM را با پورت باز بالا ببر و مستقیم از IP با **Caddy** یا **certbot** گواهی بگیر. ولی **PWA و میکروفون روی HTTPS با IP خالی کار نمی‌کنند** (مرورگر «secure context» را با IP نمی‌پذیرد). پس اگر نصب PWA / شناسایی با میکروفون مهم است، B1 (دامنه) لازم است.

---

## بعد از بالا آمدن — دو کار مهم

### ۱. بات‌وال یوتیوب از آی‌پی دیتاسنتر

یوتیوب آی‌پی‌های ابری را **سخت‌تر** بات تشخیص می‌دهد. متادیتا از کاتالوگ‌های عمومی (Apple/Deezer/Spotify) کار می‌کند، ولی **دانلود فایل صوتی از یوتیوب ممکن است** به «sign in to confirm you're not a bot» بخورد. راهش که خودِ پروژه از قبل پشتیبانی می‌کند:

- یک فایل کوکی Netscape از یوتیوب (افزونه‌ی «Get cookies.txt» در مرورگر، در حالی که لاگین هستی) بگیر.
- در VM بگذارش در `secrets/cookies.txt`.
- در `.env` باز کن: `UNSTREAM_COOKIES_FILE=/secrets/cookies.txt`.
- `docker compose up -d` (کوکی سوار می‌شود؛ سرور PO Token که در compose هست، گیت دوم را باز نگه می‌دارد).

اگر باز هم نخورد، `UNSTREAM_PROXY` (یک پروکسی residential) آخرین راه است. **این را زود تست کن، نه آخر کار** — اگر دانلود از آی‌پی ابری کلاً ممکن نباشد، ارزش دیپلوی ابری زیر سؤال می‌رود.

### ۲. کلیدهای اختیاری (همه در `.env`)

از `.env.docker.example` کپی کن و هرکدام را خواستی باز کن: Spotify (پلی‌لیست/لینک)، Genius (متن آهنگ)، AcoustID/AudD (شناسایی)، Gemini (چت وایب)، Telegram bot. بدونشان اپ بالا می‌آید، فقط لاغرتر.

---

## دستورهای روزمره (روی VM)

```bash
cd ~/unstream
docker compose --profile tunnel up -d --build   # بیلد + بالا (اولین بار کند)
docker compose --profile tunnel up -d           # بالا بدون بیلد
docker compose logs -f                          # همه‌ی لاگ‌ها
docker compose --profile tunnel logs -f tunnel  # فقط آدرس تونل
docker compose down                             # خاموش
docker compose restart server                   # ری‌استارت بک‌اند
```

## مانیتورینگ / آپ‌تایم

برای اینکه بفهمی سایت هنوز بالاست (VMهای رایگان گاهی reclaim می‌شوند):

- **UptimeRobot / Better Stack** (رایگان): هر ۵ دقیقه آدرس عمومی را ping می‌کند، اگر افتاد ایمیل می‌زند.
- `docker compose ps` روی VM وضعیت کانتینرها را نشان می‌دهد.
