# چک‌لیستِ انتشار — پلی‌استور / توزیعِ خودمیزبان

تاریخِ آماده‌سازی: نسخه‌ی ۱٫۲ (versionCode 3).

## ۱. امضا — انجام شد ✅
- کلید: `android/unstream.jks` (alias `unstream`, RSA ۲۰۴۸، اعتبار ۱۰۰۰۰ روز)
- رمزها: `android/keystore.properties` — هر دو در `.gitignore`، هیچ‌وقت کامیت نشوند
- بکاپِ کلید: `~/Documents/unstream-keystore-backup.jks` — **همین حالا یک نسخه‌ی
  ابری/آفلاینِ دوم هم بگیر.** گم‌کردنِ کلید = مرگِ همیشگیِ آپدیت‌ها
- SHA-256 گواهی: `12811523cd3592192c0b59778eda667032668cc49b12cfec82c3d9476abfcde8`
- ⚠ نسخه‌های ۱٫۰ و قبل با کلید *debug* امضا شده بودند؛ کاربرهای فعلی باید یک‌بار
  حذف و نصب کنند. متنش داخلِ `notes`ِ `latest.json` است و بنرِ داخلِ اپ نشان می‌دهد.

## ۲. انتشارِ خودمیزبان — انجام شد ✅
```bash
UNSTREAM_RELEASE_NOTES="…" node scripts/android-release.mjs --apk --publish
# مقصد: UNSTREAM_RELEASES_DIR (روی سرور: DATA_DIR/releases/)
```
- `GET /api/release` و `GET /api/release/apk` روی سرورِ زنده تست شدند (بایت‌به‌بایت یکسان)
- دروازه: تا APK امضا نشده باشد `--publish` با exit 1 جلو می‌گیرد
- `mapping.txt` کنارِ هر نسخه می‌ماند — برای رمزگشاییِ stacktraceهای
  `POST /api/client-error` لازم است

## ۳. اسکرین‌شات‌ها — انجام شد ✅
`docs/store-screenshots/` — پنج تصویر ۱۰۸۰×۱۹۲۰ (حداقلِ پلی‌استور ۱۰۸۰×۱۹۲۰،
۲ تا ۸ عدد):

| فایل | صحنه |
|---|---|
| `01-home.png` | خانه، انگلیسی LTR (هیرو + میکس روزانه + VIBE + تازه‌گرفته‌ها) |
| `02-library.png` | کتابخانه، فارسی RTL |
| `03-liked.png` | لایک‌ها، فارسی RTL |
| `04-identify.png` | صفحه با مودالِ شناسایی (اتاق/میکروفون) |
| `05-search.png` | نتایجِ جستجو، فارسی |

بازتولید (dev روی ۵۱۷۴ + API روی ۸۰۰۰ باید بالا باشد):
```bash
"$EDGE" --headless=new --window-size=1080,1920 --hide-scrollbars \
  --virtual-time-budget=15000 --lang=fa-IR \
  --screenshot=out.png "http://127.0.0.1:5174/library=1"
```
قبل از آپلود: لوگو/نامِ سرویس‌های موسیقی در هیرو را با سیاستِ پلی‌استور چک کن؛
اگر رد شد، همان اسکرین‌شات را از نسخه‌ی وبِ بدونِ آن ردیف بگیر.

## ۴. Data Safety — پیش‌نویس (فرمِ کنسول را خودت پر کن)
مبنی بر کدِ واقعیِ اپ:

- **جمع‌آوری داده:** هیچ. ثبت‌نام/آنالیتیکس/Sentry نداریم.
- **احساسِ جمع‌آوری توسطِ ثالث:** `/api/client-error` فقط وقتی کاربر سرورِ
  *خودیِ خودش* را ست کرده باشد پر می‌شود — پیام/stack/UA/آدرسِ صفحه، روی همان
  سرور، سقفِ ۵۱۲KB. «سرورِ خودِ کاربر» را در فرم «not shared with anyone»
  انتخاب کن.
- **رمزنگاری در انتقال:** شبکه‌ی شخصی (http روی LAN) — فرم می‌پرسد «in transit
  encrypted؟»؛ صادقانه: خیر برای حالتِ LAN، بله اگر کاربر HTTPS/TLS ست کرده باشد.
- **حذفِ داده:** کاربر با پاک‌کردنِ `DATA_DIR/logs/client.jsonl` روی سرور خودش.
- **کودکان:** نه. رده‌بندی سنی: ۱۲+ (لینک‌های بیرونی به سرویس‌های استریم).

## ۵. باقی‌مانده‌ی انسانی (از داخلِ کد شدنی نیست)
- [ ] حسابِ Play Console ($25) — اگر واقعاً می‌خواهی؛ برای توزیعِ تلگرامی لازم نیست
- [ ] آپلودِ AAB: `node scripts/android-release.mjs --bundle --publish` نه —
      برای استور `bundleRelease` + Play App Signing (کلیدِ آپلود جدا) لازم است
- [ ] تستِ دستی روی گوشی (حداقل یک Xiaomi/Samsung):
  - [ ] پخش + قفلِ صفحه ۱۰ دقیقه → وسطِ آهنگ نمی‌خوابد؟
  - [ ] تایمرِ خواب ۱ دقیقه‌ای با صفحه‌ی خاموش → دقیق آهنگ را بند می‌آورد؟
  - [ ] کشیدنِ نوتیفیکیشن → سرویس کامل می‌میرد (عمدی است؛ دوباره پخش کنی برمی‌گردد)
  - [ ] بنرِ بروزرسانی → دانلود در مرورگر → نصبِ درجا روی نسخه‌ی ۱٫۲ بعدی
  - [ ] میان‌بُرهای لانچر (انگشت‌فشردِ آیکون): جستجو/ادامه/لایک‌ها
  - [ ] ردیفِ باتری → «روشن‌کردن» صفحه‌ی درست را باز می‌کند؟
- [ ] کامیتِ تغییرات (درختِ کاری الان کارِ موازیِ دیگری هم دارد — با دقت stage کن)

## ۶. گیت‌هایِ فعلی (تازه، ۲۱:۳۱)
`hermes verify --skip-start --json` → ok:true · vitest 278/34 · pytest 842 ·
`assembleRelease` با R8+lintVital سبز · APK امضاشده با `apksigner verify` تأیید شد.
